diff --git a/backend/prisma/migrations/20260814000000_add_order_type/migration.sql b/backend/prisma/migrations/20260814000000_add_order_type/migration.sql
new file mode 100644
index 00000000..38696dcb
--- /dev/null
+++ b/backend/prisma/migrations/20260814000000_add_order_type/migration.sql
@@ -0,0 +1,13 @@
+-- CreateEnum
+DO $$ BEGIN
+  CREATE TYPE "OrderType" AS ENUM ('DELIVERY', 'STORE_PICKUP');
+EXCEPTION
+  WHEN duplicate_object THEN NULL;
+END $$;
+
+-- AlterTable
+-- Existing rows default to DELIVERY, since that is the only fulfillment
+-- path the checkout flow has ever supported. Revenue previously miscounted
+-- as "Boutique" (any order with shippingCost = 0, including free-shipping
+-- deliveries) will now correctly show as DELIVERY revenue.
+ALTER TABLE "Order" ADD COLUMN IF NOT EXISTS "orderType" "OrderType" NOT NULL DEFAULT 'DELIVERY';
diff --git a/backend/prisma/schema.prisma b/backend/prisma/schema.prisma
index 92b6ae0b..6552c168 100644
--- a/backend/prisma/schema.prisma
+++ b/backend/prisma/schema.prisma
@@ -30,6 +30,11 @@ enum OrderStatus {
   RETURNED
 }
 
+enum OrderType {
+  DELIVERY      // Placed through checkout, shipped to customer
+  STORE_PICKUP  // Placed/collected in person at the boutique
+}
+
 enum PaymentStatus {
   PENDING
   COMPLETED
@@ -305,6 +310,7 @@ model Order {
   userId         String?
   user           User?       @relation(fields: [userId], references: [id])
   status         OrderStatus @default(PENDING)
+  orderType      OrderType   @default(DELIVERY)
   totalAmount    Float
   shippingCost   Float       @default(0)
   promoCode      String?
diff --git a/backend/src/admin/admin.service.ts b/backend/src/admin/admin.service.ts
index 4e350ff1..163bcbde 100644
--- a/backend/src/admin/admin.service.ts
+++ b/backend/src/admin/admin.service.ts
@@ -26,11 +26,11 @@ export class AdminService {
           include: { items: true },
         }),
         this.prisma.order.aggregate({
-          where: { shippingCost: { gt: 0 } },
+          where: { orderType: 'DELIVERY' },
           _sum: { totalAmount: true },
         }),
         this.prisma.order.aggregate({
-          where: { shippingCost: { equals: 0 } },
+          where: { orderType: 'STORE_PICKUP' },
           _sum: { totalAmount: true },
         }),
       ]);
diff --git a/backend/src/orders/orders.service.ts b/backend/src/orders/orders.service.ts
index 90c89451..1410c3ce 100644
--- a/backend/src/orders/orders.service.ts
+++ b/backend/src/orders/orders.service.ts
@@ -89,6 +89,7 @@ export class OrdersService {
         data: {
           idempotencyKey: key,
           userId: userId ?? null,
+          orderType: 'DELIVERY', // only fulfillment path checkout currently supports
           totalAmount,
           shippingCost: shipping,
           promoCode: dto.promoCode ?? null,
