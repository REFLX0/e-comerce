name: Deploy specpart to AWS EC2

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  SERVER_HOST: 16.16.122.174
  SERVER_USER: ubuntu
  DEPLOY_DIR: /home/ubuntu/e-comerce
  IMAGE_PREFIX: specpart
  DOMAIN: specpart.tech

jobs:
  deploy:
    name: Deploy to AWS EC2
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        continue-on-error: true
        with:
          host: ${{ env.SERVER_HOST }}
          username: ${{ env.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY || secrets.ORACLE_SSH_KEY }}
          script: |
            set -e
            echo "=== Deploying specpart to AWS EC2 (specpart.tech) ==="
            
            cd ${{ env.DEPLOY_DIR }}
            
            # Pull latest changes
            git fetch origin main
            git reset --hard origin/main
            
            # Update containers with latest changes
            docker compose up -d --build nginx backend frontend
            
            # Clean up dangling images to save disk space
            docker image prune -f
            
            echo "=== Deployment successful: https://specpart.tech ==="

